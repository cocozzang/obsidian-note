---
id: a7b8c9d0-e1f2-3a4b-5c6d-7e8f9a0b1c2d
aliases:
  - 지수로그1
  - 거듭제곱근
  - 지수법칙
  - 로그
tags:
  - 지수
  - 로그
  - 거듭제곱근
  - 지수법칙
  - 로그성질
lecture: 큐스터디 썡기초수학
title: 16강 지수와 로그
topics:
  - 거듭제곱근
  - 지수법칙의 확장
  - 로그의 정의
  - 로그의 성질
  - 상용로그
---

# 16. 지수와 로그

## 01 지수

### (1) 거듭제곱근

#### ① 정의

제곱하여 $a$가 되는 수를 $a$의 **제곱근**이라 합니다.

즉, 방정식 $x^n = a$를 만족하는 $x$를 $a$의 **$n$제곱근**이라고 합니다.

---

#### ② $n$이 홀수일 때

모든 실수 $a$에 대해 $x^n = a$의 실근은 **1개**

이를 $\sqrt[n]{a}$ 또는 $a^{1/n}$으로 나타냅니다.

```
     y
     │
   ──┼──  y = xⁿ (n: 홀수)
     │   
     │   ╱
─────┼───────── x
     │ ╱
   ──┼──
```

**예**:
- $\sqrt[3]{8} = 2$ (왜냐하면 $2^3 = 8$)
- $\sqrt[3]{-8} = -2$ (왜냐하면 $(-2)^3 = -8$)
- $\sqrt[5]{32} = 2$

---

#### ③ $n$이 짝수일 때

**$a > 0$일 때**: 실근 **2개** (양수, 음수)

양의 실근을 $\sqrt[n]{a}$로 나타내고, 음의 실근은 $-\sqrt[n]{a}$

**$a = 0$일 때**: 실근 **1개** ($x = 0$)

**$a < 0$일 때**: 실근 **없음**

```
     y
     │
   ──┼──  y = xⁿ (n: 짝수)
     │╲ ╱
     │ ╳
─────┼───────── x
     │ ╲╱
     │
```

**예**:
- $\sqrt[4]{16} = 2$ (양의 4제곱근만)
- $\sqrt{9} = 3$ (양의 제곱근)
- $\sqrt[4]{-16}$은 실수 범위에서 존재하지 않음

---

### 거듭제곱근 정리표

| $n$ (홀수/짝수) | $a$의 부호 | 실근 개수 | 표기 |
|----------------|----------|----------|------|
| 홀수 | 모든 $a$ | 1개 | $\sqrt[n]{a}$ |
| 짝수 | $a > 0$ | 2개 | $\pm\sqrt[n]{a}$ |
| 짝수 | $a = 0$ | 1개 | $0$ |
| 짝수 | $a < 0$ | 0개 | 없음 |

---

### 📝 예제 1

다음을 계산하여라.

(1) $\sqrt[3]{27}$
(2) $\sqrt[4]{81}$
(3) $\sqrt[3]{-64}$

**풀이**:

(1) $\sqrt[3]{27} = 3$ ($3^3 = 27$)

(2) $\sqrt[4]{81} = 3$ ($3^4 = 81$)

(3) $\sqrt[3]{-64} = -4$ ($(-4)^3 = -64$)

---

### ※ $\sqrt[n]{a^n}$의 값

#### $n$이 홀수일 때
$$\sqrt[n]{a^n} = a$$

#### $n$이 짝수일 때
$$\sqrt[n]{a^n} = |a| = \begin{cases}
a & (a \geq 0) \\
-a & (a < 0)
\end{cases}$$

**예**:
- $\sqrt[3]{(-2)^3} = -2$
- $\sqrt{(-3)^2} = \sqrt{9} = 3 = |-3|$
- $\sqrt{x^2} = |x|$

---

### 📝 예제 2

다음을 간단히 하여라.

(1) $\sqrt[3]{(-5)^3}$
(2) $\sqrt{(x-2)^2}$ (단, $x < 2$)

**풀이**:

(1) $n = 3$ (홀수)

$$\sqrt[3]{(-5)^3} = -5$$

(2) $n = 2$ (짝수), $x < 2$이므로 $x - 2 < 0$

$$\sqrt{(x-2)^2} = |x-2| = -(x-2) = 2-x$$

---

### (2) 거듭제곱근의 성질

$m$, $n$이 2 이상의 자연수, $a > 0$, $b > 0$일 때:

#### ① $(\sqrt[n]{a})^n = a$

#### ② $\sqrt[n]{a} \times \sqrt[n]{b} = \sqrt[n]{ab}$

#### ③ $\frac{\sqrt[n]{a}}{\sqrt[n]{b}} = \sqrt[n]{\frac{a}{b}}$

#### ④ $(\sqrt[n]{a})^m = \sqrt[n]{a^m}$

#### ⑤ $\sqrt[m]{\sqrt[n]{a}} = \sqrt[mn]{a}$

---

### 📝 예제 3

다음을 간단히 하여라.

(1) $\sqrt[3]{2} \times \sqrt[3]{4}$

(2) $\sqrt[4]{16} \div \sqrt[4]{2}$

(3) $(\sqrt[3]{5})^6$

**풀이**:

(1) $\sqrt[3]{2} \times \sqrt[3]{4} = \sqrt[3]{2 \times 4} = \sqrt[3]{8} = 2$

(2) $\frac{\sqrt[4]{16}}{\sqrt[4]{2}} = \sqrt[4]{\frac{16}{2}} = \sqrt[4]{8}$

(3) $(\sqrt[3]{5})^6 = \sqrt[3]{5^6} = \sqrt[3]{(5^2)^3} = 5^2 = 25$

또는: $(\sqrt[3]{5})^6 = ((\sqrt[3]{5})^3)^2 = 5^2 = 25$

---

### (3) 지수법칙의 확장

#### ① 자연수 지수

$m$, $n$이 자연수일 때:
- $a^m \times a^n = a^{m+n}$
- $a^m \div a^n = a^{m-n}$
- $(a^m)^n = a^{mn}$
- $(ab)^n = a^n b^n$

---

#### ② 정수 지수로 확장

$a^0 = 1$ (단, $a \neq 0$)

$a^{-n} = \frac{1}{a^n}$ (단, $a \neq 0$)

---

#### ③ 유리수 지수로 확장

$a > 0$, $m$, $n$이 자연수일 때:

$$a^{1/n} = \sqrt[n]{a}$$

$$a^{m/n} = \sqrt[n]{a^m} = (\sqrt[n]{a})^m$$

---

### 📝 예제 4

다음을 계산하여라.

(1) $8^{1/3}$

(2) $16^{3/4}$

(3) $27^{-2/3}$

**풀이**:

(1) $8^{1/3} = \sqrt[3]{8} = 2$

(2) $16^{3/4} = \sqrt[4]{16^3} = (\sqrt[4]{16})^3 = 2^3 = 8$

(3) $27^{-2/3} = \frac{1}{27^{2/3}} = \frac{1}{(\sqrt[3]{27})^2} = \frac{1}{3^2} = \frac{1}{9}$

---

### 📝 예제 5

$$\left(\frac{1}{4}\right)^{-1/2} \times 2^{-2} \div 8^{-1/3}$$

를 계산하여라.

**풀이**:

$$\left(\frac{1}{4}\right)^{-1/2} = 4^{1/2} = 2$$

$$2^{-2} = \frac{1}{4}$$

$$8^{-1/3} = \frac{1}{8^{1/3}} = \frac{1}{2}$$

$$2 \times \frac{1}{4} \div \frac{1}{2} = \frac{2}{4} \times 2 = \frac{1}{2} \times 2 = 1$$

---

## 02 로그

### (1) 로그의 정의

$a > 0$, $a \neq 1$, $M > 0$일 때:

$$a^p = M \quad \Leftrightarrow \quad p = \log_a M$$

**읽기**: "$p$는 $a$를 밑으로 하는 $M$의 로그"

```
aᵖ = M  ⟺  p = logₐ M
↑   ↑      ↑    ↑   ↑
밑 지수   결과  지수 밑 진수
```

**용어**:
- $a$: 밑(base)
- $M$: 진수(argument)
- $p$: 로그값

**조건**:
- 진수 $M > 0$
- 밑 $a > 0$, $a \neq 1$

---

### 📝 예제 6

다음에서 $x$를 구하여라.

(1) $\log_2 8 = x$
(2) $\log_3 x = 2$
(3) $\log_x 16 = 4$

**풀이**:

(1) $\log_2 8 = x$ ⟺ $2^x = 8 = 2^3$

따라서 $x = 3$

(2) $\log_3 x = 2$ ⟺ $3^2 = x$

따라서 $x = 9$

(3) $\log_x 16 = 4$ ⟺ $x^4 = 16 = 2^4$

따라서 $x = 2$

---

### 📝 예제 7

$\log_5 x$가 정의되기 위한 $x$의 범위를 구하시오.

**풀이**:

진수 조건: $x > 0$

따라서 $x > 0$

---

### (2) 로그의 기본 성질 (공식 - 암기 필수)

$a > 0$, $a \neq 1$, $M > 0$, $N > 0$, $k$는 실수일 때:

#### ① $\log_a 1 = 0$

$a^0 = 1$이므로

#### ② $\log_a a = 1$

$a^1 = a$이므로

#### ③ $\log_a MN = \log_a M + \log_a N$

**곱 → 합**

#### ④ $\log_a \frac{M}{N} = \log_a M - \log_a N$

**나눗셈 → 차**

#### ⑤ $\log_a M^k = k\log_a M$

**지수 → 계수**

#### ⑥ 밑 변환 공식

$$\log_a M = \frac{\log_b M}{\log_b a}$$

특히: $\log_a b = \frac{1}{\log_b a}$

#### ⑦ 역함수 관계

$$\log_a b \times \log_b c = \log_a c$$

#### ⑧ $a^{\log_a M} = M$

#### ⑨ $\log_a a^k = k$

---

### 📝 공식 증명

#### ③ $\log_a MN = \log_a M + \log_a N$

$\log_a M = p$, $\log_a N = q$라 하면:

$a^p = M$, $a^q = N$

$$MN = a^p \times a^q = a^{p+q}$$

$$\log_a MN = p + q = \log_a M + \log_a N$$

---

#### ④ $\log_a \frac{M}{N} = \log_a M - \log_a N$

$$\frac{M}{N} = \frac{a^p}{a^q} = a^{p-q}$$

$$\log_a \frac{M}{N} = p - q = \log_a M - \log_a N$$

---

#### ⑥ 밑 변환 공식

$\log_a M = p$라 하면 $a^p = M$

양변에 $\log_b$를 취하면:

$$\log_b a^p = \log_b M$$
$$p \log_b a = \log_b M$$
$$p = \frac{\log_b M}{\log_b a}$$

따라서 $\log_a M = \frac{\log_b M}{\log_b a}$

---

### 📝 예제 8

다음을 간단히 하여라.

(1) $\log_2 8 + \log_2 4$

(2) $\log_3 27 - \log_3 9$

(3) $\log_5 125^2$

**풀이**:

(1) $\log_2 8 + \log_2 4 = \log_2 (8 \times 4) = \log_2 32 = \log_2 2^5 = 5$

(2) $\log_3 27 - \log_3 9 = \log_3 \frac{27}{9} = \log_3 3 = 1$

(3) $\log_5 125^2 = 2\log_5 125 = 2\log_5 5^3 = 2 \times 3 = 6$

---

### 📝 예제 9

$\log 2 = 0.3010$, $\log 3 = 0.4771$일 때, 다음을 구하여라.
(단, 상용로그)

(1) $\log 6$
(2) $\log 8$
(3) $\log 1.5$

**풀이**:

(1) $\log 6 = \log(2 \times 3) = \log 2 + \log 3$
$$= 0.3010 + 0.4771 = 0.7781$$

(2) $\log 8 = \log 2^3 = 3\log 2 = 3 \times 0.3010 = 0.9030$

(3) $\log 1.5 = \log \frac{3}{2} = \log 3 - \log 2$
$$= 0.4771 - 0.3010 = 0.1761$$

---

### 📝 예제 10

방정식 $\log_2(x+3) + \log_2(x-1) = 3$을 풀어라.

**풀이**:

$$\log_2(x+3)(x-1) = 3$$
$$(x+3)(x-1) = 2^3 = 8$$
$$x^2 + 2x - 3 = 8$$
$$x^2 + 2x - 11 = 0$$

근의 공식:
$$x = \frac{-2 \pm \sqrt{4 + 44}}{2} = \frac{-2 \pm \sqrt{48}}{2} = \frac{-2 \pm 4\sqrt{3}}{2} = -1 \pm 2\sqrt{3}$$

진수 조건:
- $x + 3 > 0$ → $x > -3$
- $x - 1 > 0$ → $x > 1$

따라서 $x > 1$

$-1 - 2\sqrt{3} < 0$이므로 제외

답: $x = -1 + 2\sqrt{3}$

---

## 03 상용로그

### (1) 정의

**상용로그**: 10을 밑으로 하는 로그

$$\log_{10} M = \log M$$

(밑 10을 생략)

---

### (2) 지표와 가수

$$\log M = n + \alpha$$

여기서:
- $n$: **지표** (정수 부분)
- $\alpha$: **가수** ($0 \leq \alpha < 1$)

---

### ① 지표 (자릿수 관련)

**$\log M$의 지표가 $n$이면**:

- $n \geq 0$일 때: $M$은 $(n+1)$자리 정수
- $n < 0$일 때: $M$은 소수점 아래 $|n+1|$번째 자리에서 처음으로 0이 아닌 수

**예**:
- $\log 2345 = 3.xxxx$ → 지표 3 → 4자리 수
- $\log 0.00234 = -3 + 0.xxxx = \overline{3}.xxxx$ → 지표 -3 → 소수점 아래 3번째

---

### ② 가수 (숫자 배열)

**가수가 같다** ⟺ **소숫점 위치와 상관없이 숫자 배열이 같다**

**예**:
- $\log 234 = 2.3692$
- $\log 23.4 = 1.3692$
- $\log 2.34 = 0.3692$
- $\log 0.234 = \overline{1}.3692 = -1 + 0.3692$

가수는 모두 $0.3692$로 같음

---

### 📝 예제 11

$\log 2 = 0.3010$일 때, 다음을 구하여라.

(1) $\log 200$
(2) $\log 0.02$

**풀이**:

(1) $\log 200 = \log(2 \times 100) = \log 2 + \log 100$
$$= 0.3010 + 2 = 2.3010$$

지표: 2, 가수: 0.3010

(2) $\log 0.02 = \log \frac{2}{100} = \log 2 - \log 100$
$$= 0.3010 - 2 = -1.6990 = \overline{2}.3010$$

지표: -2, 가수: 0.3010

---

### 📝 예제 12

$\log 3$의 지표와 가수가 이차방정식 $x^2 - px + q = 0$의 두 근일 때, 상수 $p$의 값을 구하시오.

**풀이**:

$\log 3 = 0.4771$

지표: 0, 가수: 0.4771

근과 계수의 관계:
$$p = 0 + 0.4771 = 0.4771$$

---

### (3) 자릿수와 최고자리수

#### ① 자릿수

$\log N = n.\alpha$ (지표가 $n$)일 때:

- $n \geq 0$: $N$은 $(n+1)$자리 정수
- $n < 0$: 소수점 아래 $|n+1|$번째에서 처음 0 아닌 수

---

#### ② 최고자리수

$\log N = n.\alpha$일 때:

가수 $\alpha$에 해당하는 수가 최고자리수

**예**: $\log N = 2.3010$

$\log 2 = 0.3010$이므로 최고자리수는 2

---

### 📝 예제 13

$2^{100}$의 자릿수와 최고자리수를 구하여라.
(단, $\log 2 = 0.3010$)

**풀이**:

$$\log 2^{100} = 100 \log 2 = 100 \times 0.3010 = 30.10$$

지표: 30 → 자릿수: $30 + 1 = 31$자리

가수: 0.10

$\log 1.26 \approx 0.10$이므로 최고자리수는 1

답: 31자리, 최고자리수 1

---

### 📝 예제 14

$5^{20} \times 2^{10}$은 소수점 아래 몇 번째 자리에서 처음으로 0이 아닌 숫자가 나타나는가?
(단, $\log 2 = 0.3010$, $\log 5 = 0.6990$)

**풀이**:

$$\log(5^{20} \times 2^{10}) = 20\log 5 + 10\log 2$$
$$= 20 \times 0.6990 + 10 \times 0.3010$$
$$= 13.98 + 3.01 = 16.99$$

어라? 이건 큰 수인데...

문제 재확인: $5^{-20} \times 2^{-10}$으로 추정

$$\log(5^{-20} \times 2^{-10}) = -20\log 5 - 10\log 2$$
$$= -20 \times 0.6990 - 10 \times 0.3010$$
$$= -13.98 - 3.01 = -16.99 = \overline{17}.01$$

지표: -17

소수점 아래 $|-17+1| = 16$번째에서 처음 0이 아닌 수

답: 16번째

---

## 🔑 핵심 정리

### 거듭제곱근

| $n$ | 실근 개수 |
|-----|----------|
| 홀수 | 1개: $\sqrt[n]{a}$ |
| 짝수, $a > 0$ | 2개: $\pm\sqrt[n]{a}$ |
| 짝수, $a < 0$ | 0개 |

### 지수법칙

$$a^{m/n} = \sqrt[n]{a^m}$$

### 로그의 정의

$$a^p = M \Leftrightarrow p = \log_a M$$

### 로그의 성질

| 공식 | 의미 |
|------|------|
| $\log_a MN = \log_a M + \log_a N$ | 곱 → 합 |
| $\log_a \frac{M}{N} = \log_a M - \log_a N$ | 나눗셈 → 차 |
| $\log_a M^k = k\log_a M$ | 지수 → 계수 |
| $\log_a M = \frac{\log_b M}{\log_b a}$ | 밑 변환 |

### 상용로그

- **지표**: 자릿수 결정
- **가수**: 숫자 배열 (항상 같음)

---

## 📚 연습문제

### 문제 1

다음을 계산하여라.

(1) $27^{2/3}$
(2) $(\frac{1}{8})^{-2/3}$

---

### 문제 2

방정식 $\log_3 x = -2$를 만족하는 $x$의 값을 구하여라.

---

### 문제 3

$\log 2 = 0.3010$, $\log 3 = 0.4771$일 때:

(1) $\log 12$
(2) $\log 0.6$

---

### 문제 4

$\log_2 3$의 정수부분은 $a$가 되고 $\log_3 2$의 정수부분은 $b$이다. 
$a + b$의 값을 구하시오.

---

### 문제 5

$3^{50}$의 자릿수를 구하여라.
($\log 3 = 0.4771$)

---

## 🔗 관련 링크

- [[15-series|15강: 무한급수]] - 이전 주제
- [[17-exponent-log-function|17강: 지수함수와 로그함수]] - 다음 주제

---

*다음 챕터: [[17-exponent-log-function|17강 지수함수와 로그함수]]*
